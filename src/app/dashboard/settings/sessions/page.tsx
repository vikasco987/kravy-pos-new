import SessionsClient from "./SessionsClient";

export const metadata = {
  title: "Active Sessions & Devices | Kravy POS",
};

export default function SessionsPage() {
  return <SessionsClient />;
}
