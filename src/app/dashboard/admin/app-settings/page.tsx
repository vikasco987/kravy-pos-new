import AppSettingsClient from "./AppSettingsClient";

export const metadata = {
  title: "App Settings - Kravy POS",
  description: "Manage app update settings.",
};

export default function AppSettingsPage() {
  return <AppSettingsClient />;
}
